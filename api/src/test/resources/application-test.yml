server:
  port: 8082

spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:15:///api

app:
  jwt: sometesttokenabcdefghijklmnopqrstuvxyzabcdefghijklmnopqrstuvxyzabcdefghijklmnopqrstuvxyzabcdefghijklmnopqrstuvxyz

  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: ERROR
    org:
      # enable following lines to log sql parameters for hibernate 6.0 queries
      hibernate:
        orm:
          jdbc:
            bind: TRACE
        SQL: DEBUG